[project]
name = "jev-plays-pokemon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy<2",
    "openai>=2.54.0",
    "pillow>=12.3.0",
    "pyboy==2.2.0",
    "typesafe-sdk>=0.6.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "repowise>=0.51.0",
    "ruff>=0.16.8",
    "ty>=0.0.82",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jev_plays_pokemon"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
src = ["src"]

[tool.ty.environment]
root = ["src"]
